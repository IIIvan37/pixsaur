sonar.projectKey=IDuchauffour_pixsaur
sonar.organization=IDuchauffour

# This is the name and version displayed in the SonarCloud UI.
sonar.projectName=Pixsaur
sonar.projectVersion=1.0

# Path is relative to the sonar-project.properties file. Replace "\" by "/" on Windows.
sonar.sources=src
sonar.tests=tests

# Encoding of the source code. Default is default system encoding
sonar.sourceEncoding=UTF-8

# JavaScript/TypeScript specific settings
sonar.javascript.lcov.reportPaths=coverage/lcov.info
sonar.typescript.tsconfigPath=tsconfig.json

# Exclusions
sonar.exclusions=**/node_modules/**,**/dist/**,**/target/**,**/*.spec.ts,**/*.spec.tsx,**/test-*.js
sonar.coverage.exclusions=**/*.spec.ts,**/*.spec.tsx,**/test-*.js,**/*.d.ts
