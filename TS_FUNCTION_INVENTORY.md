## TS Function Inventory (automated)

Le même inventaire que `docs/ts-function-inventory.md` (dossier docs est ignoré par git). Ce fichier est committable et sert de référence de base.

-- NOTE: Ce document est généré automatiquement par une analyse regex. Il faut ensuite enrichir manuellement la description des fonctions.

### Sections principales (extraites)

- src/libs/pixsaur-color/src/map/map-and-dither.ts
  - pseudoRandomVec(...) — helper
  - applyAtkinsonDither(...) — helper
  - processAtkinsonPixel(...) — helper
  - distributeAtkinsonError(...) — helper
  - findNearestPaletteColor(...) — helper
  - applyYliluoma1Dither(...) — exported
  - applyYliluoma2Dither(...) — exported
  - applyBayerDither(...) — exported
  - applyNoDither(...) — exported
  - applyFloydSteinbergDither(...) — exported
  - processFloydSteinbergPixel(...) — helper
  - distributeFloydSteinbergError(...) — helper
  - buildPalette(...) — helper
  - mapAndDither(...) — exported main entry

... (liste complète se trouve dans `docs/ts-function-inventory.md`)

## Propositions d'amélioration architecturale

- Consolidation des librairies couleur (distance, conversions) pour éviter duplications
- Décomposition de gros fichiers en modules plus petits et testables
- Normalisation des signatures et types (Vector, ColorSpace types)

-- Generated by grep extraction; review and refine.
