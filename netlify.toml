[build]
  command = """
    # Install Emscripten SDK in build directory
    git clone --depth 1 https://github.com/emscripten-core/emsdk.git ./emsdk && \
    cd emsdk && \
    ./emsdk install latest && \
    ./emsdk activate latest && \
    source ./emsdk_env.sh && \
    cd .. && \
    # Build RASM WASM
    chmod +x scripts/build-rasm-wasm.sh && \
    ./scripts/build-rasm-wasm.sh && \
    # Build the app
    pnpm build
  """
  publish = "dist"

[build.environment]
  NODE_VERSION = "20"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
