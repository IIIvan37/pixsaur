/**
 * Source tab - Image adjustments (brightness, contrast, saturation, etc.)
 */

import { ImageAdjustments } from '@/components/settings-panel/sections/image-adjustments'

export function SourceTab() {
  return <ImageAdjustments />
}
