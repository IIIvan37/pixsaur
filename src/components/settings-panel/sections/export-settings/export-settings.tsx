/**
 * Export settings component (smart component - no hooks needed, pure delegation)
 */

import { ExportSettingsView } from './export-settings-view'

export function ExportSettings() {
  return <ExportSettingsView />
}
