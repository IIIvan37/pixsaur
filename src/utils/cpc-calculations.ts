// Deprecated compatibility wrapper — re-export from the canonical domain
// module. Remove this file after updating all imports to use `@/export`.
// Removed implementation: `src/utils/cpc-calculations` is deprecated.
// Import the canonical implementation from `@/export` instead.
throw new Error(
  "@/utils/cpc-calculations removed — import from '@/export' instead"
)
